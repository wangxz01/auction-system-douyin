.env
*.log
bin/
tmp/

# =============================================================================
# 【本文件作用】backend/.gitignore（后端专属忽略规则）
# =============================================================================
# 作用：在根目录 .gitignore 之外，专门针对后端的额外忽略规则。
#
# 为什么不全写在根 .gitignore：
#   - 模块化清晰：后端的归后端，前端的归前端
#   - 局部生效：子目录的 .gitignore 只影响该目录及其子目录
#
# 各行说明：
#   .env     后端的环境变量文件（含数据库密码，绝对不能进 Git）
#   *.log    后端运行产生的日志
#   bin/     go build 生成的可执行文件目录
#   tmp/     临时文件（air 等热重载工具会用）
#
# 注意：根目录 .gitignore 已经包含了这些规则，这里是双保险。
#       即使有人误删了根目录 .gitignore，后端的敏感文件依然受保护。
# =============================================================================

