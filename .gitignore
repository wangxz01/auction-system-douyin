# Environment files
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependencies
node_modules/

# Build output
dist/
build/
bin/
tmp/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Go
*.exe
*.test
*.out

# 个人设计参考图（根目录）
/[0-9]*.jpg
/[0-9]*.jpeg
/[0-9]*.png

# =============================================================================
# 【本文件作用】.gitignore（项目根目录）
# =============================================================================
# 作用：告诉 Git 哪些文件/目录不要追踪、不要上传到 GitHub。
#
# 为什么需要它：
#   1. 安全：.env 文件里有密码，泄漏到 GitHub 等于裸奔
#   2. 体积：node_modules 动辄上百 MB，上传一次能让你怀疑人生
#   3. 干净：编辑器配置（.idea/.vscode）、系统垃圾文件（.DS_Store）跟项目无关
#   4. 平台无关：不同人电脑上生成的构建产物（dist/、bin/）没必要共享
#
# 各段说明：
#   - Environment files: 所有 .env 文件（含密码、密钥的真实配置）
#   - Logs:             日志文件
#   - Dependencies:     第三方依赖（package.json 已经记录了，重装一次就有）
#   - Build output:     编译产物（每次构建会重新生成）
#   - Editor / OS:      个人电脑的编辑器和操作系统残留文件
#   - Go:               Go 编译生成的二进制和测试产物
#
# 优先级：
#   根目录 .gitignore 全局生效；子目录还可以有自己的 .gitignore 做补充。
#   本项目同时存在 backend/.gitignore 和 frontend/.gitignore（Vite 自动生成的）。
# =============================================================================

